{
  "name": "@suishield/scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'scanner lint: no-op'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@suishield/receipt": "workspace:*",
    "jszip": "^3.10.1",
    "zod": "^3.24.1"
  }
}
